# layero-fixture-fullstack-camel

Фуллстек, объявленный `layero.json` в ЦЕЛЕВОЙ форме контракта.

**Проверяет** `T-20260826-4` — фуллстек в контракте.

В файле сразу всё, чего сегодня нет:

* `layout: "fullstack"` — ключ ещё не существует;
* `apiPrefix` вместо `api_prefix` — camelCase-алиаса нет;
* `installCommand` у обеих половин — не читается ни у одной;
* `framework: "fastapi"` у бэкенда — фреймворка нет в спеке.

✅ Закрыто 26.08.2026 (`T-20260826-4`). Все четыре применяются, вердикт —
`fullstack` / `python_web`, предупреждений в логе нет.

⚠️ `backend.installCommand` из файла УБРАН намеренно: зависимости бэкенда
ставит его образ, и платформа этот ключ не исполняет. Разбор теперь говорит об
этом вслух («пока не применяется»), а не молчит. Долг — `T-20260826-9`; когда
он закроется, ключ вернётся в фикстуру.
